---
title: "Bento Grid"
description: "A modular layout style inspired by Japanese bento boxes, organizing content into a grid of rectangular compartments."
tags: ["bento", "grid", "layout", "apple-style"]
preview:
  backgroundColor: "#f5f5f7"
  textColor: "#1d1d1f"
  fontFamily: "'SF Pro Display', sans-serif"
  accentColor: "#0071e3"
  secondaryColor: "#86868b"
  borderColor: "transparent"
  borderRadius: "24px"
  css: |
    .style-preview-wrapper {
      background-color: #f5f5f7;
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: 1fr 1fr;
      gap: 16px;
      padding: 16px;
    }
    .style-preview-wrapper h1 {
      display: none;
    }
    .style-preview-wrapper > div {
      background: white;
      border-radius: 24px;
      padding: 20px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      border: none;
    }
    .style-preview-wrapper > div:first-child {
      grid-column: span 2;
      background: linear-gradient(135deg, #0071e3 0%, #42a1ff 100%);
      color: white;
    }
    .style-preview-wrapper button {
      background: rgba(255, 255, 255, 0.2) !important;
      backdrop-filter: blur(10px);
      color: inherit !important;
      border-radius: 99px !important;
      border: 1px solid rgba(255,255,255,0.3) !important;
    }
prompt: |
  Create a web interface with a "Bento Grid" layout.

  **Visual Style**:
  - Layout: CSS Grid based, composed of rectangular cards of different sizes (1x1, 2x1, 2x2).
  - Background: Light grey (#f5f5f7) or dark mode background.
  - Cards: White (or dark grey) cards with large border-radius (20px-30px).
  - Content: Each card is a self-contained module (widget) with a clear purpose.
  
  **Typography**:
  - Font: Modern sans-serif (SF Pro, Inter).
  - Hierarchy: Large headings inside cards, visual emphasis on media.
  
  **Components**:
  - Media: Full-bleed images or videos inside cards.
  - Interactive: Hover effects scale cards slightly or play videos.

  **Vibe**:
  - Organized, modern, modular, information-dense but clean.
---

# Bento Grid

Bento Grid is a layout trend popularized by Apple (promotional materials) and Microsoft (Metro UI). It organizes content into distinct, rectangular compartments, much like a Japanese bento box.

## Design Tokens

### Layout
| Token | Value | Usage |
| :--- | :--- | :--- |
| `--grid-gap` | `24px` | Space between cards |
| `--card-radius` | `32px` | Card corner radius |
| `--card-bg` | `#ffffff` | Card background |

### Principles
*   **Modularity**: Everything is a box. Boxes can span multiple columns or rows.
*   **Hierarchy**: Size indicates importance. Larger boxes are for hero content.
*   **Visual Interest**: Mix text-heavy cards with image-heavy cards.

## Usage Example

```css
.bento-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-auto-rows: 300px;
  gap: 24px;
}

.bento-card {
  background: white;
  border-radius: 32px;
  padding: 32px;
  overflow: hidden;
}

.bento-card.wide {
  grid-column: span 2;
}

.bento-card.tall {
  grid-row: span 2;
}
```
